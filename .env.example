# Optional: only needed for the Textract comparison leg.
# Leave unset to run Docling-only locally.

AWS_REGION=us-east-1
AWS_PROFILE=default

# If you use explicit keys instead of a profile:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
